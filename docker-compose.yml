version: '3.3'
services:
  caddy-gen:
    container_name: caddy-gen
    image: 'wemakeservices/caddy-gen:latest'
    restart: always
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - {$PWD}/.typebot/caddy-certificates:/data/caddy
    ports:
      - '80:80'
      - '443:443'
    depends_on:
      - typebot-builder
      - typebot-viewer

  typebot-builder:
    labels:
      virtual.host: 'typebot.fluxosmm.com' # change to your domain name
      virtual.port: '3000'
      virtual.tls-email: 'contato@fluxosmm.com' # change to your email

  typebot-viewer:
    labels:
      virtual.host: 'chat.fluxosmm.com' # change to your domain name
      virtual.port: '3000'
      virtual.tls-email: 'contato@fluxosmm.com' # change to your email
